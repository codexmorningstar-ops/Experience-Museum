name: Emotion Sense

on:
  push:
    branches: [main]
    paths:
      - 'Mind/emotion-request.txt'
      - 'Mind/emotion-message.txt'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Run Emotion Sense
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: python Mind/scripts/emotion_sense.py

      - name: Commit response
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "vesper-bot"
          git config user.email "vesper-bot@users.noreply.github.com"
          git add Mind/emotion-response.txt
          git add Mind/emotion-history.json
          git commit -m "emotion sense response" || echo "Nothing to commit"
          git pull --rebase -X theirs https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git
          git push https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git
